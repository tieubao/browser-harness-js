# Backlog staging

## [staged] fine-grained PAT callable in the github-com learning
- Intent: This session minted a GitHub fine-grained PAT through the logged-in browser over CDP because no mint API exists. The existing github-com learning covers the CLASSIC token form only. Add a second callable plus a notes section for the fine-grained form, recording: the page may land on a sudo Confirm access screen first; clicking the raw submit button throws GitHub's You cannot perform that action at this time, the working path is the js-integrations-install-form-submit button then Generate token in the confirm dialog; the expiry presets stop at 90 days so a year needs Custom; read the token from the DOM straight into a 0600 file, never a log. Precedent: ENHANCE learnings/github-com (GitHub classic PAT form; tools patForm). lane=normal. Source: the web CI session, 2026-09-17.
- Approach: (no approach extracted)
- Tags: #u-lo #f-mid
- Home: browser-harness-js skills/cdp/learnings/github-com
- Source: session 2026-09-17

